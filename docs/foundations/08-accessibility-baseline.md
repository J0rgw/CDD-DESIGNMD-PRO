# Accessibility Baseline

> Status: placeholder. Body to be written in a later session.

**Purpose** — Treat accessibility as a first-class invariant, not a
post-hoc audit step.

## Planned table of contents

- Floor levels (WCAG AA as default, AAA where required)
- Contrast as a token-tier obligation
- Focus, motion, and reduced-motion contracts
- Keyboard and screen reader semantics
- Cross-references with `invariants`
- Anti-patterns specific to a11y
