# Component Contracts

> Status: placeholder. Body to be written in a later session.

**Purpose** — Specify the public surface of components — props, slots,
states, variants — so REFACTOR and AUDIT have a stable target.

## Planned table of contents

- What a contract covers (props, slots, states, a11y roles, events)
- Variants vs props vs composition (when to choose which)
- Component-tier tokens and where they live
- Versioning a component contract
- Cross-references with `tokenTiers` and `themingAxes`
- Anti-patterns specific to contracts
