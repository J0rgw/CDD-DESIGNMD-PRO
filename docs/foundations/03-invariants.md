# Invariants

> Status: placeholder. Body to be written in a later session.

**Purpose** — Declare the non-negotiable rules of the system, so the
auditor can fail loudly when they are broken.

## Planned table of contents

- What qualifies as an invariant (vs. preference)
- Common families
  - regulatory (ISA-101 for SCADA, financial disclosure colors)
  - accessibility floors (WCAG AA / AAA contrast, focus visibility)
  - brand law (logo clearspace, restricted color usage)
- DESIGN.md schema: the `invariants` extension
- Severity model
- How AUDIT and REFACTOR treat invariants
