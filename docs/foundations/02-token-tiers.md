# Token Tiers

> Status: placeholder. Body to be written in a later session.

**Purpose** — Make the primitive → semantic → component hierarchy
explicit, so every token has one and only one tier.

## Planned table of contents

- Why tiers (collapsed tiers as the root of token rot)
- Tier definitions
  - primitive (raw, brand-neutral)
  - semantic (intent-bearing, theme-aware)
  - component (locally scoped to a component contract)
- Reference rules: who may reference whom
- DESIGN.md schema: the `tokenTiers` extension
- Migration path from a flat token system
- Anti-patterns specific to tiers
