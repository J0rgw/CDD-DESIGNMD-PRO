# Design Tokens

> Status: placeholder. Body to be written in a later session.

**Purpose** — Establish the vocabulary and conventions for design
tokens used by every other foundation module: shape, naming, types,
references, and serialisation.

## Planned table of contents

- What a token is (and is not)
- Token types: color, dimension, typography, duration, ratio
- Naming conventions and casing
- The `{path.to.token}` reference syntax
- Serialisation: DESIGN.md front matter, DTCG JSON, Tailwind, CSS
  variables
- Token identity vs token value (why renaming is expensive)
- Cross-references to `02-token-tiers` and `05-runtime-theming`
