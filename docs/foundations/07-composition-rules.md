# Composition Rules

> Status: placeholder. Body to be written in a later session.

**Purpose** — Codify how primitives compose into patterns and patterns
into pages, so emergent behavior is intentional, not accidental.

## Planned table of contents

- Layers (primitives → patterns → pages)
- Spacing, alignment, and density propagation
- Slot model and forbidden compositions
- State propagation across composition boundaries
- Cross-references with `componentContracts`
- Anti-patterns specific to composition
