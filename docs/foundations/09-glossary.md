# Glossary

> Status: placeholder. Body to be written in a later session.

**Purpose** — Single source of truth for terminology used across
SKILL.md, the foundation modules, the extension schema, and AUDIT
output. Every term defined elsewhere is cross-referenced here.

## Planned table of contents

- Core terms: token, tier, axis, invariant, anti-pattern, drift
- Upstream-borrowed terms (with link to `@google/design.md` spec)
- CDD-specific terms (`themingAxes`, `tokenTiers`, `runtime`, etc.)
- Severity vocabulary (`error`, `warning`, `info`)
- Mode vocabulary (BOOTSTRAP, AUDIT, REFACTOR)
- Index of every defined term with link back to its home module
