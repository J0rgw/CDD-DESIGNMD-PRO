# AUDIT Report — Typical Drift Reference

- DESIGN.md: `./DESIGN.md`
- Generated by: cdd-designmd-pro 0.1.0
- Strict mode: off
- Active suppressions (audit.excludeRules): none
- Scope: `src/**/*.{ts,tsx,js,jsx,vue,svelte,css,scss}`,
  `components/**/*.{ts,tsx,js,jsx,vue,svelte}` (heuristic defaults
  applied; no `additionalPaths` or `excludePaths` overrides)

## Summary

| Severity | Count |
| -------- | ----- |
| error    | 0     |
| warning  | 4     |
| info     | 3     |

## Manual review required

No invariants with `enforcement: manual` are declared in this
DESIGN.md.

## Findings

### Warnings (4)

#### `hardcoded-color-literal` — src/components/Banner.tsx:6

Literal hex color outside of token files. Replace with a token
reference from `colors.*`.

```tsx
  return (
    <aside
      className="banner"
      style={{ background: "#FEF3C7", color: "#78350F" }}
```

#### `inline-style-attribute` — src/components/Banner.tsx:7

JSX `style` attribute carries a hardcoded color. Move the color
into the stylesheet and reference a semantic token.

```tsx
    <aside
      className="banner"
      style={{ background: "#FEF3C7", color: "#78350F" }}
    >
```

#### `hardcoded-color-literal` — src/components/Badge.module.css:3

Literal hex color outside of token files. Replace with a token
reference from `colors.*`.

```css
.badge {
  background-color: var(--colors-gray-50);
  color: #0F172A;
  border-radius: var(--rounded-sm);
```

#### `tier-inversion-soft` — src/components/Badge.module.css:2

Component reads primitive token `colors.gray-50` directly. A
semantic token covers the same intent (`colors.surface`); route
through it instead.

```css
.badge {
  background-color: var(--colors-gray-50);
  color: #0F172A;
  border-radius: var(--rounded-sm);
```

### Info (3)

#### `orphan-token` — components.caption

Component `caption` is declared in DESIGN.md but no source file in
scope references it (no import of `Caption.tsx`, no `caption` CSS
class). Candidate for removal or for an explicit usage.

#### `orphan-token` — colors.text-muted

Token `colors.text-muted` is declared in DESIGN.md but its only
consumer is the orphan `components.caption`. Effectively dead.

#### `orphan-token` — spacing.xs

Token `spacing.xs` is declared in DESIGN.md but is never
referenced inside the configured scope.

## Exit code

`0` — no finding has a severity in `audit.failOn` (currently
`[error]`). The team can merge, but the warnings and info findings
remain visible in the SARIF sidecar so reviewers see the drift.