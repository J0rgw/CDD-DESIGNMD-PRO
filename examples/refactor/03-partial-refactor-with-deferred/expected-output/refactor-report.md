# REFACTOR Report — Partial-Refactor Reference (industrial-scada)

- Plan applied: `a4b8c1d2-3e5f-4a6b-9c7d-8e9f0a1b2c3d`
- Generated by: cdd-designmd-pro 0.2.0
- Mode: apply (not dry-run)
- DESIGN.md: `./DESIGN.md`

## Summary

| Bucket                                   | Count |
| ---------------------------------------- | ----- |
| Applied transformations                  | 3     |
| Audit findings resolved by transforms    | 5     |
| Deferred items                           | 2     |
| Rejected items (invariant violations)    | 1     |
| Files modified                           | 3     |
| Residual findings (re-audit)             | 1     |

## Applied transformations

### `src/components/Button.tsx:8` — tokenize Button background

```diff
-      style={{ background: "#2563EB", color: "#F8FAFC" }}
+      style={{ background: "var(--colors-primary)", color: "var(--colors-text-on-primary)" }}
```

### `src/components/Card.tsx:7` — tokenize Card background

```diff
-      style={{ background: "#F8FAFC", color: "#0F172A" }}
+      style={{ background: "var(--colors-surface)", color: "var(--colors-text)" }}
```

### `src/components/AlarmBanner.tsx:7` — tokenize AlarmBanner background

```diff
-      style={{ background: "#7F1D1D", color: "#F8FAFC" }}
+      style={{ background: "var(--colors-status-critical)", color: "var(--colors-text-on-status-dark)" }}
```

## Deferred items

### `src/components/Card.test.tsx:14` — ambiguous-context

The literal `"#F8FAFC"` is documenting a legacy hex value inside
the test body, not a runtime style. Replacing with a token
reference would change the meaning of the assertion.

### `src/components/Card.test.tsx:15` — ambiguous-context

Same as deferred-1: the literal appears as the expected value of
an `expect(...).toBe(...)` assertion. Test code stays opaque to
the contract by design.

## Rejected items

### `src/styles/theme-override.css:8` — status-color-immutable

The tenant override of `--colors-status-critical` violates the
`status-color-immutable` invariant imported from the
industrial-scada preset. Status colors encode operational
meaning (ISA-101) and are not customizable per tenant. REFACTOR
does not auto-resolve invariant violations: the team must decide
whether to remove the override (preferred) or formally relax the
invariant in DESIGN.md (extraordinary).

## Residual findings

### `status-color-immutable` (error) — `src/styles/theme-override.css:8`

Re-audit reports the same invariant violation that REFACTOR
rejected during plan generation. Expected: rejected items are
not auto-resolved. The finding remains until a human acts.

## Suggested next steps

- Open `src/styles/theme-override.css` and either remove the
  `--colors-status-critical` redeclaration or escalate the
  invariant relaxation through the team's contract review
  process.
- Once the override is removed, re-run AUDIT to confirm the
  re-audit is clean.
- Re-run REFACTOR if any new tokenizable findings appeared in
  the meantime; the deferred test-file matches will still
  defer until the team decides whether to scope `*.test.tsx`
  out of audit (preferred) or rewrite the assertions.

## Exit code

`1` — `audit.failOn: [error]` includes the residual
`status-color-immutable` finding. CI must block this branch
until the override is resolved.