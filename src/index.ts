/**
 * CDD-DesignMD-Pro
 *
 * Public entry point. Implementation lands in later sessions.
 * For now this file exists so the strict TypeScript config has at
 * least one input and so downstream consumers can import the package
 * name without a resolution error.
 */

export const SKILL_NAME = "cdd-designmd-pro" as const;
export const SKILL_VERSION = "0.1.0" as const;
export const SUPPORTED_DESIGNMD_SPEC = "alpha" as const;
